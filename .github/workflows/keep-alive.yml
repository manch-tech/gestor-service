name: Keep Alive - Outro App

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase wybgqdreqylrojdxijey
        run: |
          curl -f "https://wybgqdreqylrojdxijey.supabase.co/auth/v1/health" \
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}"